#pragma once

namespace sdk {
  struct quaver_screen_type {
    enum { none, editor, gameplay, loading, menu, results, select, importing, alpha, download, lobby, multiplayer, music, theatre, initialization };
  };
} // namespace sdk